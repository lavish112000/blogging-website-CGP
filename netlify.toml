# Netlify configuration for Next.js App Router
[build]
  command = "node scripts/generate-auth0-config.mjs && npm run build"
  publish = ".next"

[dev]
  command = "next dev"

[[plugins]]
  package = "@netlify/plugin-nextjs"
